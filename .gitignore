node_modules

.vite/

.bkit*

.vscode*

.cursor*

.cursorrules*

.cursorignore*

.pnpm-store*

.claude*

.worktrees*

# macOS
.DS_Store

# Local experiment / scratch (not part of the shipping monorepo)
Dasida-boot35/

# Production deploy secrets (commit template only: deploy/.env.prod.example)
deploy/.env.prod
deploy/.env.prod.local

# Env files anywhere (docker compose auto-reads root .env; API secrets ride env vars)
.env
.env.*
!.env*.example
!*.env.example

# Ad-hoc Gradle/API tooling logs
apps/api/build-*.log

# Local uploaded media (API disk store)
uploads/

# Playwright MCP session artifacts (local browser automation traces)
.playwright-mcp/
